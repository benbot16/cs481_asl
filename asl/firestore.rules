rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /user_profiles/{doc_id} {
      allow read, update, delete: if request.auth != null && request.auth.uid == doc_id;
      allow create: if request.auth != null;
    }
  }
}
